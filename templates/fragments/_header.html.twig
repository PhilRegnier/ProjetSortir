<header>
    <a  class="main-title" href="{{ path('main_connecte') }}">
        <img class="logo-sortir" src="{{ asset('ressources/images/logo-sortir.jpg') }}" alt="sortir.com logo">
    </a>
    <div class="links">
    {% if not app.user %}
        <a class="link" href="{{ path('app_register') }}">S'inscrire</a>
        <a class="link" href="{{ path('app_login') }}">Se connecter</a>
    {% endif %}
    {% if app.user %}
        <a class="link" href="{{ path('main_connecte') }}">Accueil</a>
        <a class="link" href="{{ path('participant_monprofil') }}">Mon profil</a>
        <a class="link" href="{{ path('app_logout') }}">Se déconnecter</a>
        {% if is_granted('ROLE_ADMIN') %}
            <a  class="link" href="{{ path('admin') }}">admin</a>
        {% endif %}
    {% endif %}
    </div>
</header>